apiVersion: v1
kind: Service
metadata:
  name: natesclaw
  labels:
    app: natesclaw
spec:
  type: ClusterIP
  selector:
    app: natesclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
