interface:
  display_name: "Natesclaw Release CI"
  short_description: "Verify and debug Natesclaw release validation runs"
  default_prompt: "Use $release-natesclaw-ci to validate one frozen release SHA with one active parent run, recover only the smallest failed lane, and stop on a precise external blocker."
